<h1><%= @meat.name %></h1>

<ul>
	<li>Price: <%= @meat.price %></li>
</ul>

<%= link_to 'Edit', edit_meat_path %>
<%= link_to 'Delete', @meat, method: :delete, data: {confirm: 'Are you sure?'} %>

<p><%= link_to 'Back', meats_path %></p>