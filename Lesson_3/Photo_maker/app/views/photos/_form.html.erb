<%= form_for @photo do |p| %>

  <p><%= p.label :name %></p>
  <%= p.text_field :name %>

  <p><%= p.label :price %></p>
  <%= p.text_field :price %>

  <p><%= p.submit %></p>

<% end %>

<%= link_to 'Back', photos_path %>